<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="#">About bank</a>
		</div>
		<div id="navbar" class="navbar-collapse collapse">
			<ul class="nav navbar-nav">
				<%= header_main_links :individuals %>
				<%= header_main_links :companies %>
				<%= header_main_links :banks %>
			</ul>
			<ul class="nav navbar-nav navbar-right">
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= current_locale_name %> <span class="caret"></span></a>
					<ul class="dropdown-menu" role="menu">
						<% locale_list.each do |local_key, locale_name| %>
							<li><%= link_to locale_name, { locale: local_key } %></li>
						<% end %>
					</ul>
				</li>
		
				<% if logged_in? %>
					<li id="fat-menu" class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= "#{current_user.first_name} #{current_user.last_name}" %> <b class="caret"></b></a>
						<ul class="dropdown-menu">
							<li><%= link_to I18n.t("session.profile"), current_user %></li>
							<li class="divider"></li>
							<li>
								<%= link_to I18n.t("session.logout"), logout_path, method: "delete" %>
							</li>
						</ul>
					</li>
				<% else %>
			        <li><%= link_to I18n.t("main_pages.signup"), signup_path %></li>
					<li><%= link_to I18n.t("main_pages.login"), login_path %></li>
				<% end %>
			</ul>
		</div><!--/.nav-collapse -->
	</div>
</nav>